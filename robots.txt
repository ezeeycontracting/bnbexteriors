# PREVIEW / STAGING robots.txt — BLOCKS ALL CRAWLERS.
# This file ships with the *.pages.dev preview so it is NOT indexed.
# AT GO-LIVE: replace this file's contents with robots-production.txt
# (which allows crawling and references the sitemap).
User-agent: *
Disallow: /
